@carloscanutocosta:registry=https://npm.pkg.github.com
# O token de autenticação deve ser configurado no ~/.npmrc do utilizador:
# //npm.pkg.github.com/:_authToken=<GITHUB_TOKEN>
# Em CI, o token é injetado via variável de ambiente GITHUB_TOKEN pelos GitHub Actions.
